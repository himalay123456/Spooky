const viewCollection = [...Array(24)].map((_, index) => ({
  id: index,
  collectionName: 'Steph Curry Chase Center 2021',
  category: 'Sports',
  creator: '@creator',
  owner: '@owner',
  actions: 'delete',
}));

export default viewCollection;
