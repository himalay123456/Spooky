/* eslint-disable import/prefer-default-export */
import { makeStyles } from '@material-ui/core/styles';

const AnalyticsStyles = makeStyles(() => ({
  paddingAll: {
    padding: '0px 20px',
  },
}));

export { AnalyticsStyles };
